pluginManagement {
	repositories {
		maven { url = 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
        maven {
            url = uri("https://packages.confluent.io/maven/")
        }
	}
}
rootProject.name = 'base-domains'
